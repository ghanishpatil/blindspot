"""Risk stage.

Two separate concerns, kept separate on purpose:

* current cryptographic weakness (is it broken today?)
* quantum migration urgency via Mosca's inequality (must we migrate now?)

Implemented in Phase 8.
"""
